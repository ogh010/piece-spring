version: '3'

services:
  piece-react:
    image: ${DOCKER_IMAGE_NAME}
    container_name: piece-react
    ports:
      - "${PORT}:${PORT}"
    environment:
      - ACTIVE_PROFILE=${PROFILE}
    restart: unless-stopped