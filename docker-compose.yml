version: '3'

services:
  formdang-sp-api:
    image: ${DOCKER_IMAGE_NAME}
    container_name: piece-spring
    ports:
      - "${PORT}:${PORT}"
    networks:
      - server_connection
    environment:
      - ACTIVE_PROFILE=${PROFILE}
    restart: unless-stopped